<h1>{{blog.title}}</h1>
{% for article in blog.articles %}
    <div>
        <a href="{{article.url}}">
        <h2>
            {{article.title}}
            </h2>
            <h6>{{article.author}}</h6>
            <p>{{article.excerpt}}</p>
        </a>
    </div>
{% endfor %}